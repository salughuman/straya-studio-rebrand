---
/**
 * Button.astro — Straya Studio
 * Visual style: inset-shadow highlight, border, shadow-xs, active press-down.
 * Extracted from base-ui button pattern — adapted to Astro + Tailwind, no new deps.
 */
interface Props extends astroHTML.JSX.AnchorHTMLAttributes {
  href: string;
  label: string;
  variant?: 'primary' | 'secondary' | 'outline' | 'ghost' | 'hero-pill' | 'text-link';
  size?: 'sm' | 'md' | 'lg';
  iconCircle?: boolean;
}

const {
  href,
  label,
  variant = 'primary',
  size = 'md',
  iconCircle = false,
  class: className,
  ...rest
} = Astro.props;
---

{variant === 'hero-pill' ? (
  <a
    href={href}
    {...rest}
    class:list={[
      "group inline-flex items-center gap-2 self-start rounded-full py-1 pl-5 pr-1 text-sm font-medium no-underline select-none",
      "bg-[#E1E0CC] text-[#0A0A0A]",
      // Inset highlight + outer shadow — the base-ui treatment
      "shadow-[inset_0_1px_0_rgba(255,255,255,0.6),0_2px_8px_rgba(0,0,0,0.28)]",
      "transition-[gap,box-shadow] duration-300 ease-[cubic-bezier(0.16,1,0.3,1)]",
      "hover:gap-3 hover:shadow-[inset_0_1px_0_rgba(255,255,255,0.6),0_4px_16px_rgba(0,0,0,0.36)]",
      // Active press — shadow collapses inward
      "active:shadow-[inset_0_1px_0_rgba(0,0,0,0.1),0_1px_2px_rgba(0,0,0,0.2)]",
      "focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent focus-visible:ring-offset-2 focus-visible:ring-offset-[#0A0A0A]",
      "disabled:pointer-events-none disabled:opacity-[0.64]",
      "sm:text-base",
      className,
    ]}
  >
    {label}
    {iconCircle ? (
      <span class="flex h-9 w-9 items-center justify-center rounded-full bg-black shadow-[inset_0_1px_0_rgba(255,255,255,0.08)] transition-transform duration-300 ease-[cubic-bezier(0.16,1,0.3,1)] group-hover:scale-110 sm:h-10 sm:w-10">
        <slot />
      </span>
    ) : (
      <slot />
    )}
  </a>
) : variant === 'text-link' ? (
  <a
    href={href}
    {...rest}
    class:list={[
      "group inline-flex items-center gap-2 px-0 pb-px",
      "font-medium text-sm tracking-[0.04em] uppercase no-underline select-none",
      "text-on-background/60",
      // M3 underline draw — grows from 0 to 100% on hover
      "bg-[linear-gradient(currentColor,currentColor)] bg-[length:0%_1px] bg-no-repeat bg-[position:0_100%]",
      "transition-[color,background-size] duration-240 ease-[cubic-bezier(0.16,1,0.3,1)]",
      "hover:text-on-background hover:bg-[length:100%_1px]",
      "focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent focus-visible:ring-offset-2 focus-visible:ring-offset-background",
      "disabled:pointer-events-none disabled:opacity-[0.64]",
      className,
    ]}
  >
    <span class="flex items-center gap-2">
      <span>{label}</span>
      <slot />
    </span>
  </a>
) : (
  <a
    href={href}
    {...rest}
    class:list={[
      // Base — shared across all non-pill, non-link variants
      "btn-base group relative inline-flex shrink-0 cursor-pointer items-center justify-center gap-2 whitespace-nowrap rounded-lg border font-medium no-underline select-none",
      "transition-shadow duration-150 ease-[cubic-bezier(0.16,1,0.3,1)]",
      // Focus ring
      "focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent focus-visible:ring-offset-1 focus-visible:ring-offset-background",
      // Disabled
      "disabled:pointer-events-none disabled:opacity-[0.64]",
      // Before pseudo — inset highlight layer
      "before:pointer-events-none before:absolute before:inset-0 before:rounded-[calc(0.5rem-1px)]",

      // ── Variant styles ──────────────────────────────────────
      variant === 'primary' && [
        "border-primary bg-primary text-on-primary",
        // Inset top highlight + outer shadow
        "shadow-[inset_0_1px_0_rgba(255,255,255,0.16),0_1px_3px_rgba(0,0,0,0.24)]",
        // Hover
        "hover:bg-primary/90",
        // Active — inset flips, outer shadow drops
        "active:shadow-[inset_0_1px_0_rgba(0,0,0,0.08)]",
      ],

      variant === 'secondary' && [
        "border-secondary bg-secondary text-on-secondary",
        "shadow-[inset_0_1px_0_rgba(255,255,255,0.16),0_1px_3px_rgba(0,0,0,0.24)]",
        "hover:bg-secondary/90",
        "active:shadow-[inset_0_1px_0_rgba(0,0,0,0.08)]",
      ],

      variant === 'outline' && [
        "border-on-background/12 bg-background text-on-background",
        // Subtle outer shadow + inner highlight via before
        "shadow-[0_1px_2px_rgba(0,0,0,0.06)] before:shadow-[inset_0_1px_0_rgba(0,0,0,0.06)]",
        "hover:bg-on-background/5 hover:border-on-background/20",
        "active:shadow-none",
      ],

      variant === 'ghost' && [
        "border-transparent bg-transparent text-on-background",
        "hover:bg-on-background/8",
        "active:bg-on-background/12",
      ],

      // ── Size styles ─────────────────────────────────────────
      size === 'sm' && "h-8 gap-1.5 px-[calc(0.625rem-1px)] text-xs tracking-[0.04em] uppercase",
      size === 'md' && "h-9 px-[calc(0.75rem-1px)] text-sm tracking-[0.04em] uppercase sm:h-8",
      size === 'lg' && "h-10 px-[calc(0.875rem-1px)] text-base tracking-[0.03em] uppercase sm:h-9",

      className,
    ]}
  >
    <span class="relative z-10 flex items-center justify-center gap-2">
      <span>{label}</span>
      <slot />
    </span>
  </a>
)}

<style>
  a { -webkit-tap-highlight-color: transparent; }
</style>
